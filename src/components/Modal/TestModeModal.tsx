import React from 'react';
import styled from './TestModeModal.module.scss'

const TestModeModal = () => {
  return (
    <div className={styled.modalContainer}>
      hi
    </div>
  );
};

export default TestModeModal;